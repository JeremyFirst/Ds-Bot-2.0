# 🤖 Discord Bot для Административной Инфраструктуры Rust-сервера

[![Python](https://img.shields.io/badge/Python-3.8+-blue.svg)](https://www.python.org/)
[![Discord.py](https://img.shields.io/badge/discord.py-2.3+-blue.svg)](https://github.com/Rapptz/discord.py)
[![MySQL](https://img.shields.io/badge/MySQL-5.7+-orange.svg)](https://www.mysql.com/)
[![License](https://img.shields.io/badge/License-MIT-green.svg)](LICENSE)

Автоматизированная система управления административными привилегиями Rust-сервера через Discord. Бот синхронизирует привилегии из игры с ролями Discord через RCON, обеспечивая централизованное управление администрацией.

## 📋 Описание

Этот Discord-бот является частью административной инфраструктуры Rust-сервера и предназначен для:

- **Автоматической синхронизации привилегий** из Rust-сервера (Oxide) с ролями Discord
- **Управления списком администрации** через интерактивный Embed
- **Централизованного логирования** всех действий администрации
- **Автоматического обновления** ролей при изменении привилегий на сервере

### ✨ Основные возможности

- 🔄 Автоматическая синхронизация привилегий через RCON
- 📊 Интерактивный Embed со списком администрации
- 🔔 Автоматическое обновление при изменении ролей
- 📝 Детальное логирование всех действий
- 🛡️ Проверка прав доступа для команд
- ⚡ Восстановление сообщений при перезапуске
- 🌍 Поддержка временных зон (UTC/UTC+3)

## 🚀 Быстрый старт

### Требования

- **Python** 3.8 или выше
- **MySQL** 5.7+ или **MariaDB** 10.3+
- **Discord Bot Token** с правами:
  - `Manage Roles`
  - `Send Messages`
  - `Embed Links`
  - `Read Message History`
- **Доступ к RCON** Rust-сервера

### Установка

1. **Клонируйте репозиторий:**
```bash
git clone https://github.com/yourusername/rust-admin-bot.git
cd rust-admin-bot
```

2. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

3. **Настройте переменные окружения:**
```bash
# Скопируйте шаблон
copy env.example .env

# Отредактируйте .env и заполните значения
```

4. **Настройте конфигурацию:**
```bash
# Отредактируйте config.yml
# Укажите ID каналов, ролей и другие параметры
```

5. **Создайте базу данных MySQL:**
```sql
CREATE DATABASE admin_log_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

6. **Запустите бота:**
```bash
python bot.py
```

База данных инициализируется автоматически при первом запуске.

### Запуск на Pterodactyl

Если вы используете панель Pterodactyl, см. подробную инструкцию в файле [PTERODACTYL_SETUP.md](PTERODACTYL_SETUP.md).

**Быстрая настройка:**
1. Настройте переменные окружения в панели (см. [PTERODACTYL_SETUP.md](PTERODACTYL_SETUP.md))
2. Используйте startup command с автоматическим клонированием:
```bash
cd /home/container && if [[ ! -d .git ]] || [[ ! -f bot.py ]]; then echo "Клонирование репозитория..."; rm -rf * .[^.]* 2>/dev/null || true; git clone https://github.com/JeremyFirst/Ds-Bot-2.0.git .; fi && if [[ -d .git ]] && [[ "${AUTO_UPDATE}" == "1" ]]; then git pull; fi && if [[ -f requirements.txt ]]; then pip install -U --prefix .local -r requirements.txt; fi && /usr/local/bin/python bot.py
```

Эта команда автоматически клонирует репозиторий при первом запуске.

## ⚙️ Конфигурация

### Переменные окружения (.env)

Создайте файл `.env` на основе `env.example`:

```env
# Discord Bot Token
DISCORD_BOT_TOKEN=your_discord_bot_token_here

# MySQL Database
DB_HOST=localhost
DB_PORT=3306
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_NAME=admin_log_db

# RCON Configuration
RCON_HOST=localhost
RCON_PORT=28016
RCON_PASSWORD=your_rcon_password
```

### Конфигурационный файл (config.yml)

```yaml
discord:
  staff_channel_id: 123456789012345678  # ID канала для /staff
  admin_roles:
    - role_id: 111111111111111111
      name: "Модератор"
      priority: 1
    - role_id: 222222222222222222
      name: "Администратор"
      priority: 2
  high_staff_roles:
    - 333333333333333333  # Роли с доступом к /addprivilege
  command_channel_id: 123456789012345679  # Канал для уведомлений

rcon:
  timeout: 10
  retry_attempts: 3

privileges:
  groups:
    - "moderator"
    - "admin"
    - "senior_admin"
    - "owner"
```

## 📖 Команды

### `/staff`
Создает или обновляет Embed-сообщение со списком администрации сервера.

**Использование:**
```
/staff
```

**Описание:**
- Отображает все роли администрации с приоритетами
- Показывает список участников с каждой ролью
- Отображает статус участников (онлайн/оффлайн)
- Автоматически обновляется при изменении ролей

### `/staff_refresh`
Принудительно обновляет Embed-сообщение `/staff`.

**Использование:**
```
/staff_refresh
```

### `/addprivilege`
Добавляет или обновляет привилегию пользователя на основе данных с Rust-сервера.

**Использование:**
```
/addprivilege @user 76561198000000000
```

**Параметры:**
- `user` - Discord пользователь (упоминание)
- `steam_id` - SteamID игрока (формат: `STEAM_0:0:12345678` или `76561198000000000`)

**Требования:**
- Только для ролей High Staff (настраивается в `config.yml`)
- Пользователь должен быть на Discord-сервере
- Бот должен иметь права на выдачу ролей

**Логика работы:**
1. Выполняет RCON-команду `pinfo SteamID`
2. Парсит ответ и извлекает группу привилегии и дату окончания
3. Сравнивает с данными в БД
4. Если данные изменились:
   - Обновляет запись в БД
   - Выдает/обновляет Discord-роль
   - Обновляет Embed `/staff`
   - Уведомляет пользователя (ЛС или канал)
5. Если данных нет изменений - только логирует действие

## 🏗️ Архитектура проекта

```
rust-admin-bot/
├── bot.py                 # Главный файл бота
├── commands/              # Команды Discord
│   ├── staff.py          # Команда /staff
│   └── addprivilege.py   # Команда /addprivilege
├── services/             # Сервисы
│   ├── rcon.py          # RCON клиент
│   └── staff_embed.py   # Управление Embed
├── database/             # Работа с БД
│   ├── connection.py    # Подключение к MySQL
│   └── models.py        # SQLAlchemy модели
├── config/              # Конфигурация
│   └── config_loader.py # Загрузчик config.yml
├── utils/               # Утилиты
│   ├── steam.py        # Валидация SteamID
│   ├── timezone.py     # Конвертация времени
│   └── pinfo_parser.py # Парсер ответа pinfo
├── config.yml          # Конфигурация
├── requirements.txt    # Зависимости
└── README.md          # Документация
```

## 🔧 Технические детали

### База данных

Бот использует MySQL для хранения:
- **staff_messages** - информация о сообщениях `/staff`
- **user_privileges** - привилегии пользователей

Все даты хранятся в формате UTC. Конвертация в UTC+3 выполняется только для отображения пользователю.

### RCON интеграция

Бот выполняет команду `pinfo SteamID` на Rust-сервере через RCON для получения информации о привилегиях игрока. Ответ парсится для извлечения:
- Названия группы привилегии
- Даты окончания привилегии (если есть)

### Автоматическое обновление

Embed `/staff` автоматически обновляется:
- При изменении ролей у участников (`on_member_update`)
- При обновлении ролей администрации (`on_guild_role_update`)
- Периодически каждые 5 минут (проверка существования сообщения)
- При перезапуске бота (восстановление удаленных сообщений)

### Логирование

Все действия логируются в файл `bot.log` и консоль:
- **ACTION** - успешные операции (создание/обновление привилегий)
- **ERROR** - ошибки (RCON, БД, Discord API)
- **INFO** - информационные сообщения

## 🛠️ Разработка

### Структура кода

Проект следует принципам:
- **Модульность** - каждый компонент в отдельном модуле
- **Разделение ответственности** - команды, сервисы, утилиты разделены
- **Документированность** - все функции имеют docstrings
- **Обработка ошибок** - все критические операции обернуты в try/except

### Добавление новых команд

1. Создайте файл в `commands/`
2. Создайте класс команды с методом `register_commands(tree)`
3. Зарегистрируйте команду в `bot.py`

Пример:
```python
class MyCommand:
    def __init__(self, bot):
        self.bot = bot
    
    def register_commands(self, tree):
        @tree.command(name="mycommand", description="Описание")
        async def mycommand(interaction):
            await interaction.response.send_message("Ответ")
```

## 📝 Примеры использования

### Создание списка администрации

1. Выполните команду `/staff` в нужном канале
2. Бот создаст Embed со списком всех администраторов
3. Список будет автоматически обновляться при изменении ролей

### Добавление привилегии

1. Убедитесь, что у вас есть роль High Staff
2. Выполните `/addprivilege @user 76561198000000000`
3. Бот проверит привилегию на сервере и обновит роль в Discord

## ❓ FAQ

**Q: Бот не подключается к Discord**  
A: Проверьте правильность `DISCORD_BOT_TOKEN` в `.env` и права бота на сервере.

**Q: Ошибка подключения к MySQL**  
A: Убедитесь, что MySQL запущен и параметры в `.env` корректны. База данных должна существовать.

**Q: RCON команды не выполняются**  
A: Проверьте `RCON_HOST`, `RCON_PORT` и `RCON_PASSWORD`. Убедитесь, что RCON включен на сервере.

**Q: Роли не выдаются**  
A: Проверьте, что бот имеет роль выше целевой роли и права "Manage Roles".

**Q: Embed не обновляется автоматически**  
A: Убедитесь, что бот имеет права на чтение и редактирование сообщений в канале.

## 🤝 Вклад в проект

Приветствуются любые улучшения! Пожалуйста:
1. Создайте Fork проекта
2. Создайте ветку для новой функции (`git checkout -b feature/AmazingFeature`)
3. Закоммитьте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Запушьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. См. файл `LICENSE` для подробностей.

## 👤 Автор

**Ваше имя**
- GitHub: [@JeremyFirst]

## 🙏 Благодарности

- [discord.py](https://github.com/Rapptz/discord.py) - Discord API для Python
- [SQLAlchemy](https://www.sqlalchemy.org/) - ORM для работы с БД
- [Oxide](https://umod.org/) - Плагин-фреймворк для Rust

---
